import { connect } from "react-redux";
import UtilsTablerIconsScreen from "./view";

const mapStateToProps = () => ({});

const mapDispatchToProps = () => ({});

export default connect(mapStateToProps, mapDispatchToProps)(UtilsTablerIconsScreen);
