import { connect } from "react-redux";
import UtilsShadowScreen from "./view";

const mapStateToProps = () => ({});

const mapDispatchToProps = () => ({});

export default connect(mapStateToProps, mapDispatchToProps)(UtilsShadowScreen);
